import 'package:flutter/material.dart';

const Color primaryColor = Color(0xFF0C0F0A);
const primaryPadding = EdgeInsets.only(left: 50, right: 50);
const allPadding = EdgeInsets.all(8.0);

class AppTexts {
  static const String wentWrong = 'Oops! Something went wrong.';
}

class AppColors {
  static const Color primary = Color(0xFFFFFFFF);
  static const Color secondary = Color(0xFF0C0F0A);
  static const Color pinkish = Color(0xFFFF206E);
  static const Color yellow = Color(0xFFFBFF12);
  static const Color blue = Color(0xFF41EAD4);
  static const Color grey = Color(0xFF757575);
}
